/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "static.wixstatic.com","corporate.walmart.com",
        ],
      },
};

export default nextConfig;
